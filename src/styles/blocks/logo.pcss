.logo {
  color: $color-white;
  text-transform: uppercase;
  font-size: 36px;
  font-weight: 800;
  line-height: 1;
  transition: 0.3s;
  &:hover {
    color: $color-orange;
  }
  @include tablets {
    width: 50%;
  }
}
