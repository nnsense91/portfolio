mixin nav(itemName,mod)
    nav(class="navigation navigation--"+mod)
        ul.navigation__list
            - for (var i=0; i<itemName.length; i++)
                li.navigation__item 
                    a(href="#").navigation__link=`${itemName[i]}`